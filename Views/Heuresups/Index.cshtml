@model IEnumerable<PFE.Models.Heuresup>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Dureehs)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IddetailNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Dureehs)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IddetailNavigation.Iddetail)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idhs">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idhs">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idhs">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

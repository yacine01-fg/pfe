@model IEnumerable<PFE.Models.Planning>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nomplanning)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Horairedebut)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Horairefin)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nomplanning)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Horairedebut)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Horairefin)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Idplanning">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Idplanning">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Idplanning">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
